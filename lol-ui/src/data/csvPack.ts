// src/data/csvPack.ts
// Stub temporaire: compile sans CSV (on tombera sur le BasePack)
export async function loadCsvDataPack(_base = "/csv") {
  return { teams: [], players: [], staff: [], scheduleWeeks: [], fixtures: undefined };
}
